CREATE  TABLE `bdrevista`.`REVISTA` (
  `id` INT NOT NULL ,
  `nome` VARCHAR(45) NOT NULL ,
  `ano` VARCHAR(45) NOT NULL ,
  `mes` VARCHAR(45) NOT NULL ,
  `qtdpg` VARCHAR(45) NOT NULL ,
  PRIMARY KEY (`id`) );
